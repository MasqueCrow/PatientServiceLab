Usage instructions
1. Run init.bat to initialize all the required node modules for the web application
(ONLY REQUIRED TO RUN FOR THE FIRST TIME)
2. Run run.bat to start the service. Close the command window to end the service.
3. Access the webservice through http://localhost:3000/patients/{nric}.